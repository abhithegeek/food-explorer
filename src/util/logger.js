// Class for logging all errors. For simplicity we are using console.error.
// In real-life application you would want to log this to server instead.

export default function logError(message) {
  console.error(message);
};